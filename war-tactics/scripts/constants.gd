class_name Constants
extends RefCounted

const XP_PER_KILL: int = 25
const HEAL_ALL_XP_COST: int = 50
const MAP_NODE_COUNT: int = 3
const BASE_ENEMY_COUNT: Array[int] = [3, 4, 5]
const ENEMY_HP_SCALE: Array[float] = [1.0, 1.25, 1.5]

const LEVEL_DESCRIPTIONS: Array[String] = [
	"Open Field — scattered cover, light resistance",
	"Urban Ruins — tight corridors, moderate resistance",
	"Hilltop Siege — enemies hold the high ground",
]

[gd_scene load_steps=4 format=3]

; World-space health bar: a dark background quad with a coloured fill quad in front of it. An art
; pass resizes the quads here; WorldHpBar reads the fill width from the quad, so no code changes.
; The materials are per-instance and are built in code, because the fill colour is tweened.

[ext_resource type="Script" path="res://scripts/combat/WorldHpBar.cs" id="1_hpbar"]

[sub_resource type="QuadMesh" id="QuadMesh_hpbg"]
size = Vector2(0.84, 0.13)

[sub_resource type="QuadMesh" id="QuadMesh_hpfill"]
size = Vector2(0.8, 0.09)

[node name="WorldHpBar" type="Node3D"]
script = ExtResource("1_hpbar")

[node name="HpBarBg" type="MeshInstance3D" parent="."]
cast_shadow = 0
mesh = SubResource("QuadMesh_hpbg")

[node name="HpFill" type="MeshInstance3D" parent="."]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0, 0.001)
cast_shadow = 0
mesh = SubResource("QuadMesh_hpfill")
